const en = require('./en.json')
const de = require('./de.json')
const nl = require('./nl.json')
const da = require('./da.json')
const ru = require('./ru.json')

const languages = {
  en,
  de,
  da,
  nl,
  ru,
}

/**
 * Exports all language locales
 */
export default languages
